# CORE CONTEXT MATERIAL

> Pending Dev Team repo initialization.

Run `.logitaka/builders/BUILD_CONTEXT_MATERIAL.md` in your IDE/VDA and replace `.logitaka/CORE_CONTEXT_MATERIAL.md` with the generated report.
After the builder commits and pushes the result, confirm the commit back in Logitaka so it can import the context and remove the builder files.
